# Jarwin Configuration
# Optional: Add OpenAI key for better quality (works without it too)
# OPENAI_API_KEY=sk-your-key-here

# LLM Provider: "openai" or "local" (default: local - no cost)
LLM_PROVIDER=local
